// Copy this file to secrets.h (gitignored). Never commit real values.

#pragma once

const char* WIFI_SSID = "YOUR_WIFI_2.4GHZ";
const char* WIFI_PASSWORD = "YOUR_WIFI_PASSWORD";

// From Flutter app after device claim, or Supabase Table Editor → devices
const char* DEVICE_UID = "SOILGOOD-ESP32-01";
const char* INGEST_TOKEN = "paste_token_here";

// Project Settings → API (anon public key only — never service_role)
const char* SUPABASE_URL = "https://YOUR_PROJECT_REF.supabase.co";
const char* SUPABASE_ANON_KEY = "eyJ...YOUR_LEGACY_ANON_JWT";
